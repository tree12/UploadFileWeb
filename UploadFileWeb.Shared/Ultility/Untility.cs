using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection;
using System.Text;
using System.Threading.Tasks;
using UploadFileWeb.Shared.Constants;

namespace UploadFileWeb.Shared.Ultility
{
    public static class Untility
    {
        //public static string GetMemberValue<T>(string name)
        //{
        //    var property = typeof(T).GetField(name, BindingFlags.Public | BindingFlags.Static | BindingFlags.FlattenHierarchy);
        //    return property?.GetValue(null)?.ToString();

        //}
        //public static string GetMemberName<T>(string value)
        //{
        //    var properties = typeof(T).GetProperties();
        //    string name = string.Empty;
        //    foreach (var item in properties)
        //    {
        //        if (value == item?.GetValue(null)?.ToString())
        //            name = item.Name;
        //    }
        //    return name;

        //}
    }
}
